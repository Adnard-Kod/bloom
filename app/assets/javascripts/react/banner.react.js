/**
 * @jsx React.DOM
 */

var React = require('react');

var Banner = React.createClass({displayName: 'Banner',

  render: function() {
    return (
      React.DOM.div(null)
    );
  }

});

module.exports = Banner;
